(progn

  (setq counter 0)

  ;; Should execute with 3 2 1
  ;;TODO: the original would do 0 1 2
  (dotimes '(a 3)
    '(progn
       (message a)
       (setq counter (+ counter a))
       )

    )

  (equal counter 6))
