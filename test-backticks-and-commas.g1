(and

 ;; Simple eval
 (equal
  (let ((a 1))
    `(,a))

  '(1))

 ;; Simple eval, followed by a symbol which should not be evaluated
 (equal
  (let ((a 1)
	(b 2))
    `(,a b))

  '(1 b))



 ;; depply nested eval
 (equal
  (let ((a 1)
	(b 2))
    `(,a ((,b))))

  '(1 ((2))))

 )
