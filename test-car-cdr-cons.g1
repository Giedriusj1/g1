(progn


  ;; (message (cdr (quote (1 2 3))))

  ;; (message (cdr (quote (2 2 21 2 3))))

  ;; (message (cons (+ 1 1) (quote (1 2 3))))
  ;; (message (cons (+ 1 1) 1))


  (and

   ;; test car
   (equal (car (quote (aaa 2 3)))
	  (quote aaa))

   (equal (car (quote (1 2 3)))
	  1)


   ;; test cdr
   (equal (cdr (quote (1 2 3)))
	  (quote (2 3)))


   (equal (cdr (quote (1 2 c)))
	       (quote (2 c)))


   ;; test cons
   (equal (cons 1 (quote (2 3)))
	  (quote (1 2 3)))


   (equal (cons (quote a) (quote (2 3)))
	  (quote (a 2 3)))


   )


)
