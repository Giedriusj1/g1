(progn

  (defun 'loop '(iter body)
    '(
      (if (> iter 0)
	  (progn
	    ;; (message n)
	    ;; (message body)
	    (eval body)
	    (loop (- iter 1) body))
	)
      )
    )

  (equal (loop 10 '(progn
		     (message iter)
		     ))
	 nil)
  )
