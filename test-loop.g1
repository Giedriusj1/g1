(progn

  (setq defun
	'((name params body)
	  (set name (list
		     params
		     body
		     ))))

  (defun 'loop '(n body)
    '(
      (if (> n 1)
	  (progn
	    (message n)
	    (message body)

	    ;; (message 'hello)
	    ;; (eval body)
	    (message 'evaluatingbody)
	    (body)
	    ;; (message 'loopz)
	    (message 'evaluatingbodyDone)
	    (loop (- n 1) (quote body))
	    )
	)

      )

    )

  ;; (message loop)

  ;; (loop  11 2 )

  (loop  11 '(message 'loopz))

  ;; (eval (eval '(message 'aa)))

  ;; (message 1)




  )
